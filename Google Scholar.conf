[General]
bypass-system = true
ipv6 = false
# 这里的 url 只是为了测速，可以不改
test-timeout = 5

[Proxy Group]
# 1. 学术专用组：自动筛选名字里带 "alwaysdata" 的节点
# policy-regex-filter 里的内容就是你要匹配的关键词
🏔️ Google Scholar = select, policy-regex-filter=alwaysdata

# 2. 常规节点组：匹配所有节点 (.*)，供其他流量使用
🚀 节点选择 = select, policy-regex-filter=.*

[Rule]
# --- 学术规则 (指向上面定义的学术组) ---
DOMAIN-KEYWORD,scholar,🏔️ Google Scholar
DOMAIN-SUFFIX,scholar.google.com,🏔️ Google Scholar
DOMAIN-SUFFIX,scholar.google.cn,🏔️ Google Scholar
DOMAIN-SUFFIX,scholar.google.com.hk,🏔️ Google Scholar
DOMAIN-SUFFIX,scholar.googleusercontent.com,🏔️ Google Scholar

# --- 兜底规则 (剩下的全走常规节点) ---
# 如果你想剩下的直连，就把下面的 "🚀 节点选择" 改为 "DIRECT"
FINAL,🚀 节点选择
